---
- hosts: 
    - master
    - worker
  tags: ['init']
  become: true
  roles:
    - apt
    - docker
    - kubeadm
  gather_facts: true

- hosts:
    - master
  tags: ['deploy']
  roles:
    - master_node

- hosts:
    - worker
  tags: ['deploy']
  roles:
    - worker_node
